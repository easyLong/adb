"""Next-generation document-driven crawler application.

This package contains the next-generation document-driven crawler modules. The
whole project now defaults to the ``crawler_app`` database, so legacy workflow
tables and v2 tables can be initialized in the same empty database while the old
database remains untouched.
"""
